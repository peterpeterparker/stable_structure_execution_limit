{
  "name": "stable_structure_execution_limit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "generate": "scripts/did.sh && dfx generate && node scripts/update.types.mjs && npm run format"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.0.2",
    "vite": "^4.4.8"
  },
  "dependencies": {
    "@junobuild/core": "^0.0.32"
  }
}
